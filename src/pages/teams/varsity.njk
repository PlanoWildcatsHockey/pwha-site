---
title: Varsity
permalink: /teams/varsity/index.html
description: The Plano Wildcats Varsity hockey roster and coaching staff.
layout: page
---

<h1>Varsity</h1>

<p>
  The Plano Wildcats Varsity team competes at the highest level of the
  program. Below is the current roster, drawn from our player database.
</p>

{# Roster filtered from src/_data/players.json #}
{% set roster = players | filterCurrentTeam("varsity") %}

{% if roster.length %}
  <table>
    <thead>
      <tr>
        <th>#</th>
        <th>Name</th>
        <th>Pos</th>
        <th>Year</th>
      </tr>
    </thead>
    <tbody>
      {% for p in roster %}
        <tr>
          <td>{{ p.number }}</td>
          <td>{{ p.name }}</td>
          <td>{{ p.position }}</td>
          <td>{{ p.year }}</td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
{% else %}
  <p><em>Phase 1 placeholder — roster will populate from <code>src/_data/players.json</code> once player data is added and the <code>filterCurrentTeam</code> filter is wired up.</em></p>
{% endif %}

<h2>Coaching staff</h2>

<p><em>Phase 1 placeholder.</em> Add head coach, assistants, and managers.</p>
